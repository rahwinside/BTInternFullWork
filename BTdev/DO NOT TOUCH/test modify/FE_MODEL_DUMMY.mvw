{ safe_quotes_on }
{ MOD_0 = "FE_MODEL_DUMMY.h3d" }
{ RES_0 = "FE_MODEL_DUMMY.h3d" }
*Id("HyperWorks", "8.*")
*BeginPage()
    *Title("Analysis Results", On)
    *Layout(1)
    *BeginAnimator(Static)
        *CurrentPosition(0)
        *NumberOfSteps(4)
        *Increment(Forward, Frame, 1, BounceOff)
    *EndAnimator()
    *BeginWindow(Animation)
        *BeginGraphic()
            *BeginModel({MOD_0}, "Hyper3D Reader", 1, 1)
                *ResultSource({RES_0}, "Hyper3D Reader", 1, 1)
                *BeginPart(2, "Global", "PART", 2)
                    *Attribute(On, IdOff, 50, Opa, Sha)
                *EndPart()
                *BeginPart(3, "Global", "PART", 3)
                    *Attribute(On, IdOff, 5, Opa, Sha)
                *EndPart()
                *BeginPart(4, "Global", "PART", 4)
                    *Attribute(On, IdOff, 52, Opa, Sha)
                *EndPart()
                *BeginPart(5, "Global", "PART", 5)
                    *Attribute(On, IdOff, 5, Opa, Sha)
                *EndPart()
                *BeginPart(6, "Global", "PART", 6)
                    *Attribute(On, IdOff, 4, Opa, Sha)
                *EndPart()
                *BeginPart(9, "Global", "PART", 9)
                    *Attribute(On, IdOff, 48, Opa, Sha)
                *EndPart()
                *BeginPart(11, "Global", "PART", 11)
                    *Attribute(On, IdOff, 28, Opa, Sha)
                *EndPart()
                *BeginPart(12, "Global", "PART", 12)
                    *Attribute(On, IdOff, 36, Opa, Sha)
                *EndPart()
                *BeginPart(14, "Global", "PART", 14)
                    *Attribute(On, IdOff, 40, Opa, Sha)
                *EndPart()
                *BeginPart(15, "Global", "PART", 15)
                    *Attribute(On, IdOff, 17, Opa, Sha)
                *EndPart()
                *BeginPart(17, "Global", "PART", 17)
                    *Attribute(On, IdOff, 60, Opa, Sha)
                *EndPart()
                *BeginPart(19, "Global", "PART", 19)
                    *Attribute(On, IdOff, 8, Opa, Sha)
                *EndPart()
                *BeginPart(20, "Global", "PART", 20)
                    *Attribute(On, IdOff, 28, Opa, Sha)
                *EndPart()
                *BeginPart(21, "Global", "PART", 21)
                    *Attribute(On, IdOff, 24, Opa, Sha)
                *EndPart()
                *BeginPart(22, "Global", "PART", 22)
                    *Attribute(On, IdOff, 5, Opa, Sha)
                *EndPart()
                *BeginPart(24, "Global", "PART", 24)
                    *Attribute(On, IdOff, 20, Opa, Sha)
                *EndPart()
                *BeginPart(25, "Global", "PART", 25)
                    *Attribute(On, IdOff, 57, Opa, Sha)
                *EndPart()
                *BeginPart(27, "Global", "PART", 27)
                    *Attribute(On, IdOff, 48, Opa, Sha)
                *EndPart()
                *BeginPart(28, "Global", "PART", 28)
                    *Attribute(On, IdOff, 8, Opa, Sha)
                *EndPart()
                *BeginPart(29, "Global", "PART", 29)
                    *Attribute(On, IdOff, 44, Opa, Sha)
                *EndPart()
                *BeginPart(30, "Global", "PART", 30)
                    *Attribute(On, IdOff, 24, Opa, Sha)
                *EndPart()
                *BeginPart(31, "Global", "PART", 31)
                    *Attribute(On, IdOff, 4, Opa, Sha)
                *EndPart()
                *BeginPart(33, "Global", "PART", 33)
                    *Attribute(On, IdOff, 48, Opa, Sha)
                *EndPart()
                *BeginPart(34, "Global", "PART", 34)
                    *Attribute(On, IdOff, 8, Opa, Sha)
                *EndPart()
                *BeginPart(35, "Global", "PART", 35)
                    *Attribute(On, IdOff, 28, Opa, Sha)
                *EndPart()
                *BeginPart(36, "Global", "PART", 36)
                    *Attribute(On, IdOff, 36, Opa, Sha)
                *EndPart()
                *BeginPart(37, "Global", "PART", 37)
                    *Attribute(On, IdOff, 24, Opa, Sha)
                *EndPart()
                *BeginPart(38, "Global", "PART", 38)
                    *Attribute(On, IdOff, 40, Opa, Sha)
                *EndPart()
                *BeginPart(40, "Global", "PART", 40)
                    *Attribute(On, IdOff, 20, Opa, Sha)
                *EndPart()
                *BeginPart(42, "Global", "PART", 42)
                    *Attribute(On, IdOff, 60, Opa, Sha)
                *EndPart()
                *BeginPart(43, "Global", "PART", 43)
                    *Attribute(On, IdOff, 24, Opa, Sha)
                *EndPart()
                *BeginPart(44, "Global", "PART", 44)
                    *Attribute(On, IdOff, 40, Opa, Sha)
                *EndPart()
                *BeginPart(45, "Global", "PART", 45)
                    *Attribute(On, IdOff, 17, Opa, Sha)
                *EndPart()
                *BeginPart(46, "Global", "PART", 46)
                    *Attribute(On, IdOff, 36, Opa, Sha)
                *EndPart()
                *BeginPart(48, "Global", "PART", 48)
                    *Attribute(On, IdOff, 48, Opa, Sha)
                *EndPart()
                *BeginPart(53, "Global", "PART", 53)
                    *Attribute(On, IdOff, 59, Opa, Sha)
                *EndPart()
                *BeginPart(58, "Global", "PART", 58)
                    *Attribute(On, IdOff, 24, Opa, Sha)
                *EndPart()
                *BeginPart(60, "Global", "PART", 60)
                    *Attribute(On, IdOff, 20, Opa, Sha)
                *EndPart()
                *BeginPart(61, "Global", "PART", 61)
                    *Attribute(On, IdOff, 57, Opa, Sha)
                *EndPart()
                *BeginPart(62, "Global", "PART", 62)
                    *Attribute(On, IdOff, 55, Opa, Sha)
                *EndPart()
                *BeginPart(63, "Global", "PART", 63)
                    *Attribute(On, IdOff, 8, Opa, Sha)
                *EndPart()
                *BeginPart(64, "Global", "PART", 64)
                    *Attribute(On, IdOff, 28, Opa, Sha)
                *EndPart()
                *BeginPart(65, "Global", "PART", 65)
                    *Attribute(On, IdOff, 36, Opa, Sha)
                *EndPart()
                *BeginPart(67, "Global", "PART", 67)
                    *Attribute(On, IdOff, 20, Opa, Sha)
                *EndPart()
                *BeginPart(68, "Global", "PART", 68)
                    *Attribute(On, IdOff, 57, Opa, Sha)
                *EndPart()
                *BeginPart(72, "Global", "PART", 72)
                    *Attribute(On, IdOff, 43, Opa, Sha)
                *EndPart()
                *BeginPart(73, "Global", "PART", 73)
                    *Attribute(On, IdOff, 59, Opa, Sha)
                *EndPart()
                *BeginPart(76, "Global", "PART", 76)
                    *Attribute(On, IdOff, 44, Opa, Sha)
                *EndPart()
                *BeginPart(77, "Global", "PART", 77)
                    *Attribute(On, IdOff, 24, Opa, Sha)
                *EndPart()
                *BeginPart(79, "Global", "PART", 79)
                    *Attribute(On, IdOff, 3, Opa, Sha)
                *EndPart()
                *BeginPart(80, "Global", "PART", 80)
                    *Attribute(On, IdOff, 26, Opa, Sha)
                *EndPart()
                *BeginPart(81, "Global", "PART", 81)
                    *Attribute(On, IdOff, 49, Opa, Sha)
                *EndPart()
                *BeginPart(82, "Global", "PART", 82)
                    *Attribute(On, IdOff, 44, Opa, Sha)
                *EndPart()
                *BeginPart(83, "Global", "PART", 83)
                    *Attribute(On, IdOff, 32, Opa, Sha)
                *EndPart()
                *BeginPart(85, "Global", "PART", 85)
                    *Attribute(On, IdOff, 60, Opa, Sha)
                *EndPart()
                *BeginPart(86, "Global", "PART", 86)
                    *Attribute(On, IdOff, 44, Opa, Sha)
                *EndPart()
                *BeginPart(88, "Global", "PART", 88)
                    *Attribute(On, IdOff, 35, Opa, Sha)
                *EndPart()
                *BeginPart(89, "Global", "PART", 89)
                    *Attribute(On, IdOff, 52, Opa, Sha)
                *EndPart()
                *BeginPart(90, "Global", "PART", 90)
                    *Attribute(On, IdOff, 28, Opa, Sha)
                *EndPart()
                *BeginPart(91, "Global", "PART", 91)
                    *Attribute(On, IdOff, 28, Opa, Sha)
                *EndPart()
                *BeginPart(92, "Global", "PART", 92)
                    *Attribute(On, IdOff, 49, Opa, Sha)
                *EndPart()
                *BeginPart(93, "Global", "PART", 93)
                    *Attribute(On, IdOff, 33, Opa, Sha)
                *EndPart()
                *BeginPart(94, "Global", "PART", 94)
                    *Attribute(On, IdOff, 28, Opa, Sha)
                *EndPart()
                *BeginPart(95, "Global", "PART", 95)
                    *Attribute(On, IdOff, 36, Opa, Sha)
                *EndPart()
                *BeginPart(353, "Global", "PART", 353)
                    *Attribute(On, IdOff, 56, Opa, Sha)
                *EndPart()
                *BeginPart(355, "Global", "PART", 355)
                    *Attribute(On, IdOff, 60, Opa, Sha)
                *EndPart()
                *BeginPart(356, "Global", "PART", 356)
                    *Attribute(On, IdOff, 63, Opa, Sha)
                *EndPart()
                *BeginPart(623, "Global", "PART", 623)
                    *Attribute(On, IdOff, 36, Opa, Sha)
                *EndPart()
                *BeginPart(626, "Global", "PART", 626)
                    *Attribute(On, IdOff, 51, Opa, Sha)
                *EndPart()
                *BeginPart(628, "Global", "PART", 628)
                    *Attribute(On, IdOff, 29, Opa, Sha)
                *EndPart()
                *BeginPart(629, "Global", "PART", 629)
                    *Attribute(On, IdOff, 55, Opa, Sha)
                *EndPart()
                *BeginPart(630, "Global", "PART", 630)
                    *Attribute(On, IdOff, 40, Opa, Sha)
                *EndPart()
                *BeginPart(631, "Global", "PART", 631)
                    *Attribute(On, IdOff, 62, Opa, Sha)
                *EndPart()
                *BeginPart(635, "Global", "PART", 635)
                    *Attribute(On, IdOff, 6, Opa, Sha)
                *EndPart()
                *BeginPart(636, "Global", "PART", 636)
                    *Attribute(On, IdOff, 7, Opa, Sha)
                *EndPart()
                *BeginPart(637, "Global", "PART", 637)
                    *Attribute(On, IdOff, 62, Opa, Sha)
                *EndPart()
                *BeginPart(638, "Global", "PART", 638)
                    *Attribute(On, IdOff, 20, Opa, Sha)
                *EndPart()
                *BeginPart(644, "Global", "PART", 644)
                    *Attribute(On, IdOff, 51, Opa, Sha)
                *EndPart()
                *BeginPart(645, "Global", "PART", 645)
                    *Attribute(On, IdOff, 32, Opa, Sha)
                *EndPart()
                *BeginPart(650, "Global", "PART", 650)
                    *Attribute(On, IdOff, 20, Opa, Sha)
                *EndPart()
                *BeginPart(651, "Global", "PART", 651)
                    *Attribute(On, IdOff, 44, Opa, Sha)
                *EndPart()
                *BeginPart(652, "Global", "PART", 652)
                    *Attribute(On, IdOff, 20, Opa, Sha)
                *EndPart()
                *BeginPart(660, "Global", "PART", 660)
                    *Attribute(On, IdOff, 42, Opa, Sha)
                *EndPart()
                *BeginPart(661, "Global", "PART", 661)
                    *Attribute(On, IdOff, 5, Opa, Sha)
                *EndPart()
                *BeginPart(686, "Global", "PART", 686)
                    *Attribute(On, IdOff, 45, Opa, Sha)
                *EndPart()
                *BeginPart(1053, "Global", "PART", 1053)
                    *Attribute(On, IdOff, 52, Opa, Sha)
                *EndPart()
                *BeginPart(1054, "Global", "PART", 1054)
                    *Attribute(On, IdOff, 37, Opa, Sha)
                *EndPart()
                *BeginPart(1057, "Global", "PART", 1057)
                    *Attribute(On, IdOff, 52, Opa, Sha)
                *EndPart()
                *BeginPart(1062, "Global", "PART", 1062)
                    *Attribute(On, IdOff, 28, Opa, Sha)
                *EndPart()
                *BeginPart(1063, "Global", "PART", 1063)
                    *Attribute(On, IdOff, 32, Opa, Sha)
                *EndPart()
                *BeginPart(1071, "Global", "PART", 1071)
                    *Attribute(On, IdOff, 26, Opa, Sha)
                *EndPart()
                *BeginPart(1073, "Global", "PART", 1073)
                    *Attribute(On, IdOff, 52, Opa, Sha)
                *EndPart()
                *BeginPart(1329, "Global", "PART", 1329)
                    *Attribute(On, IdOff, 4, Opa, Sha)
                *EndPart()
                *BeginPart(1748, "Global", "PART", 1748)
                    *Attribute(On, IdOff, 3, Opa, Sha)
                *EndPart()
                *BeginPart(1752, "Global", "PART", 1752)
                    *Attribute(On, IdOff, 44, Opa, Sha)
                *EndPart()
                *BeginPart(1755, "Global", "PART", 1755)
                    *Attribute(On, IdOff, 56, Opa, Sha)
                *EndPart()
                *BeginPart(2999, "Global", "PART", 2999)
                    *Attribute(On, IdOff, 60, Opa, Sha)
                *EndPart()
                *BeginPart(3000, "Orphan-Grids", "PART", 0)
                    *Attribute(Off, IdOff, 37, Opa, Sha)
                *EndPart()
                *BeginGroup("Orphan Nodes", "Off", "Off", "255 255 255", 1, "wire")
                    *BeginSelection(Part, SelectAll, "User_Set", 3)
                        *Add("Orphan-Grids id 1")
                    *EndSelection()
                *EndGroup()
                *BeginMask()
                    *BeginSelection(Part, SelectAll, "User_Set", 0)
                        *Add("Orphan-Grids id 1")
                    *EndSelection()
                *EndMask()
            *EndModel()
            *CurrentModel(0)
            *View(Isometric)
        *EndGraphic()
    *EndWindow()
*EndPage()
