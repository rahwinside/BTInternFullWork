{ safe_quotes_on }
{ HM_MODEL_1 = "FE_model_dummy.hm"}
*Id("HyperWorks", "14.*")
*AnimationSyncPolicy(CLOSEST)
*BeginPalette()
*EndPalette()
*BeginPage() // Page 1
    *IsActive()
    *Name("page0")
    *Title("Untitled", On)
    *TitleFont("Arial", 1, 0, 12)
    *Layout(1)
    *BeginAnimator(Transient)
        *CurrentTime(Undeformed)
        *StartTime(Undefined)
        *EndTime(Undefined)
        *Increment(Forward, Frame, 1, BounceOff)
    *EndAnimator()
    *WindowIDs(0)
    *BeginWindow(FePre)         // Window 1
        *IsActive()
        *ModelStorageFlag(1)
        *SaveDataInline(0)
        *ExportFormat("PNG")
        *BeginFePre()
            *Filename({HM_MODEL_1})
        *EndFePre()
    *EndWindow()
    *TimeScales(1)
    *TimeDelays(0)
    *AnimationEnable(1)
    *SyncTolerance(2e-008)
    *SyncTableGenerationPolicy(ALL_BLOCKS)
    *PageAnimationSyncPolicy(CLOSEST)
*EndPage()
